using System;
using System.Collections.Generic;

namespace TestProject
{
    public class HUY
    {
        private readonly int length;
        private readonly string name;

        public HUY(string name, int length)
        {
            this.name = name;
            this.length = length;
        }
    }
}